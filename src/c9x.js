'use strict';

// Application entry point

var path = require('path');

var runtime;
var api;

var logger; // TODO: Inspect logger
var version;
var settings;
// let api;

function getVersion() {
    if (!version) {
        // grab version from package.json
    }
    return version;
};

module.exports = {
    
    }
}